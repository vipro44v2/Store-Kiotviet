import { UnrecoverableError,Worker } from "bullmq";import os from "node:os";import { assertProductionEnv,getEnv } from "../lib/env";import { getRedis,closeRedis } from "../lib/redis/client";import { closeDatabase,query } from "../lib/db/client";import { QUEUE_NAMES } from "../lib/queue/queues";import { processSyncJob,isManualReview } from "../lib/queue/worker";import { jobsRepository } from "../repositories/jobs";import { retryDelay } from "../lib/queue/jobs";import { webhooksRepository } from "../repositories/webhooks";
if(getEnv().NODE_ENV==="production")assertProductionEnv();const workerId=`${os.hostname()}:${process.pid}`;const workers=QUEUE_NAMES.map(name=>new Worker(name,async job=>{const auditId=String(job.data.auditJobId);await jobsRepository.start(auditId,job.attemptsMade+1);try{const result=await processSyncJob(job);await jobsRepository.complete(auditId);return result;}catch(error){const e=error instanceof Error?error:new Error(String(error)),manual=isManualReview(e);if(job.data.eventId)await webhooksRepository.markFailed(job.data.eventId,e.message);await jobsRepository.fail(auditId,e,manual);if(manual)throw new UnrecoverableError(e.message);throw e;}},{connection:getRedis(),concurrency:getEnv().WORKER_CONCURRENCY,settings:{backoffStrategy:(attemptsMade)=>retryDelay(attemptsMade)}}));
const heartbeat=setInterval(()=>void query("INSERT INTO worker_heartbeats(worker_id,last_seen_at,status,metadata) VALUES($1,now(),'healthy',$2) ON CONFLICT(worker_id) DO UPDATE SET last_seen_at=now(),status='healthy',metadata=EXCLUDED.metadata",[workerId,JSON.stringify({pid:process.pid,hostname:os.hostname()})]),15000);heartbeat.unref();
async function shutdown(){clearInterval(heartbeat);await Promise.all(workers.map(w=>w.close()));await closeRedis();await closeDatabase();}
process.on("SIGTERM",()=>void shutdown().then(()=>process.exit(0)));process.on("SIGINT",()=>void shutdown().then(()=>process.exit(0)));process.stdout.write(`${JSON.stringify({level:"info",message:"Worker started",workerId,queues:QUEUE_NAMES})}\n`);
