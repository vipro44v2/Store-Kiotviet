"use client";import { useState } from "react";import { useRouter } from "next/navigation";
export function SettingsEditor({settings}:{settings:Array<{key:string;value:Record<string,unknown>}>}){const router=useRouter();return <div className="settings-grid">{settings.map(setting=><Setting key={setting.key} name={setting.key} initial={setting.value} onSaved={()=>router.refresh()}/>)}</div>}
function Setting({name,initial,onSaved}:{name:string;initial:Record<string,unknown>;onSaved:()=>void}){const[value,setValue]=useState(JSON.stringify(initial,null,2)),[message,setMessage]=useState("");async function save(){let parsed:unknown;try{parsed=JSON.parse(value);}catch{setMessage("Invalid JSON");return;}const response=await fetch(`/api/admin/settings/${name}`,{method:"PUT",headers:{"Content-Type":"application/json"},body:JSON.stringify(parsed)});setMessage(response.ok?"Saved":"Save failed");if(response.ok)onSaved();}return <section className="setting-card"><h2>{name}</h2><textarea value={value} onChange={event=>setValue(event.target.value)} rows={10} spellCheck={false}/><div><button onClick={()=>void save()}>Save</button><small>{message}</small></div></section>}
