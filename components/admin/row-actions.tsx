"use client";
import { useRouter } from "next/navigation";
import { useState } from "react";
export function RowActions({resource,id,status}:{resource:string;id:string;status?:string}){const router=useRouter(),[message,setMessage]=useState(""),[loading,setLoading]=useState(false);let actions:Array<{key:string;label:string}>=[];if(resource==="jobs")actions=status==="manual_review"?[{key:"retry",label:"Retry"},{key:"resolve",label:"Resolve"},{key:"cancel",label:"Cancel"}]:status==="pending"||status==="failed"?[{key:"retry",label:"Retry"},{key:"cancel",label:"Cancel"}]:[];if(resource==="conflicts"&&status!=="resolved")actions=[{key:"resolved",label:"Resolve"},{key:"ignored",label:"Ignore"}];if(resource==="webhooks")actions=[{key:"reprocess",label:"Reprocess"}];if(!actions.length)return null;async function act(key:string){if(loading)return;setLoading(true);setMessage("Working...");const endpoint=resource==="jobs"?`/api/admin/jobs/${id}`:resource==="conflicts"?`/api/admin/conflicts/${id}`:`/api/admin/webhooks/${id}`,body=resource==="jobs"?{action:key}:resource==="conflicts"?{status:key,resolution:{actor:"admin"}}:{};try{const response=await fetch(endpoint,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});setMessage(response.ok?"Done":"Failed");if(response.ok)router.refresh()}catch{setMessage("Failed")}finally{setLoading(false)}}return <div className="row-actions">{actions.map(action=><button className="button button-compact button-secondary" type="button" disabled={loading} key={action.key} onClick={()=>void act(action.key)}>{action.label}</button>)}{message&&<small>{message}</small>}</div>}
