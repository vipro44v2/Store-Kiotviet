"use client";
import { useState } from "react";
export function ActionButton({action,label,variant="primary"}:{action:string;label:string;variant?:"primary"|"secondary"}){const[state,setState]=useState(""),[loading,setLoading]=useState(false);async function run(){if(loading)return;setLoading(true);setState("Queueing...");try{const response=await fetch(`/api/admin/sync/${action}`,{method:"POST"});setState(response.ok?"Queued":"Failed")}catch{setState("Failed")}finally{setLoading(false)}}return <div className="action-wrap"><button className={`button button-${variant}`} type="button" disabled={loading} onClick={()=>void run()}>{label}</button>{state&&<small>{state}</small>}</div>}
