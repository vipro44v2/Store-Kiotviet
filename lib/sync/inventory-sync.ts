import { ConflictError,MappingError } from "@/lib/errors";import { normalizeSku } from "./mappings";import { mappingsRepository } from "@/repositories/mappings";import { query } from "@/lib/db/client";import { getShopifyInventory,setShopifyInventory } from "@/lib/shopify/inventory";import { log } from "@/lib/logger";import type { KiotVietStockNotification } from "@/lib/kiotviet/types";
export function calculateInventory(onHand:number,reserved=0,safetyStock=0,allowsNegative=false){const value=Math.floor(onHand-reserved-safetyStock);return allowsNegative?value:Math.max(0,value);}
export async function syncInventoryNotification(notification:KiotVietStockNotification,jobId?:string){const sku=normalizeSku(notification.ProductCode);if(!sku)throw new MappingError("Inventory event has an empty SKU");const mappings=await mappingsRepository.findBySku(sku);if(mappings.length!==1)throw mappings.length>1?new ConflictError(`Duplicate mapping for SKU ${sku}`):new MappingError(`No mapping for SKU ${sku}`);const mapping=mappings[0];if(!mapping.shopify_inventory_item_id)throw new MappingError(`Mapping ${sku} has no Shopify inventory item`);const locations=await query<{shopify_location_id:string;safety_stock:string}>("SELECT shopify_location_id,safety_stock::text FROM branch_location_mappings WHERE kiotviet_branch_id=$1 AND enabled=true",[notification.BranchId]);if(!locations.length)throw new MappingError(`No Shopify location mapped for KiotViet branch ${notification.BranchId}`);for(const location of locations){const expected=calculateInventory(notification.OnHand,notification.Reserved,Number(location.safety_stock));const current=await getShopifyInventory(mapping.shopify_inventory_item_id,location.shopify_location_id);if(current!==expected)await setShopifyInventory(mapping.shopify_inventory_item_id,location.shopify_location_id,expected,current);await query("INSERT INTO inventory_snapshots(sku,branch_id,shopify_location_id,kiotviet_quantity,shopify_quantity,expected_shopify_quantity,difference) VALUES($1,$2,$3,$4,$5,$6,$7)",[sku,notification.BranchId,location.shopify_location_id,notification.OnHand,current,expected,current-expected]);await log("info",current===expected?"Inventory already reconciled":"Shopify inventory updated",{action:"update_shopify_inventory",provider:"shopify",entityType:"inventory",entityId:sku,sku,previous:current,next:expected,jobId});}}
