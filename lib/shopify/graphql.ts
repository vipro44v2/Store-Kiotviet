import { getEnv } from "@/lib/env";import { ApiError,AuthenticationError,RateLimitError,RetryableError } from "@/lib/errors";import type { ShopifyGraphqlResponse } from "@/types/shopify";
export async function shopifyGraphql<T>(query:string,variables:Record<string,unknown>={}):Promise<T>{const env=getEnv();if(!env.SHOPIFY_SHOP_DOMAIN||!env.SHOPIFY_ACCESS_TOKEN)throw new AuthenticationError("Shopify credentials are not configured");const endpoint=`https://${env.SHOPIFY_SHOP_DOMAIN}/admin/api/${env.SHOPIFY_API_VERSION}/graphql.json`;for(let attempt=0;attempt<4;attempt++){let response:Response;try{response=await fetch(endpoint,{method:"POST",headers:{"Content-Type":"application/json","X-Shopify-Access-Token":env.SHOPIFY_ACCESS_TOKEN},body:JSON.stringify({query,variables}),cache:"no-store",signal:AbortSignal.timeout(20000)});}catch{if(attempt<3)continue;throw new RetryableError("Shopify network request failed");}if(response.status===401||response.status===403)throw new AuthenticationError(`Shopify authentication failed (${response.status})`);if(response.status===429){if(attempt<3){await new Promise(r=>setTimeout(r,1000*(attempt+1)));continue;}throw new RateLimitError("Shopify rate limit exceeded");}if(response.status>=500){if(attempt<3)continue;throw new RetryableError(`Shopify temporary error (${response.status})`);}if(!response.ok)throw new ApiError(`Shopify HTTP error (${response.status})`,response.status);let payload:ShopifyGraphqlResponse<T>;try{payload=await response.json() as ShopifyGraphqlResponse<T>;}catch{throw new ApiError("Shopify returned invalid JSON");}if(payload.errors?.length)throw new ApiError(payload.errors.map(e=>e.message).join("; "));if(!payload.data)throw new ApiError("Shopify response contains no data");const throttle=payload.extensions?.cost?.throttleStatus;if(throttle&&throttle.currentlyAvailable<100)await new Promise(r=>setTimeout(r,Math.ceil((100-throttle.currentlyAvailable)/throttle.restoreRate*1000)));return payload.data;}throw new RetryableError("Shopify request retries exhausted");}
