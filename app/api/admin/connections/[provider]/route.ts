import { requireAdmin } from "@/lib/auth/middleware";import { getKiotVietAccessToken } from "@/lib/kiotviet/auth";import { shopifyGraphql } from "@/lib/shopify/graphql";import { query } from "@/lib/db/client";
export async function POST(_request:Request,context:{params:Promise<{provider:string}>}){try{await requireAdmin();const{provider}=await context.params;if(provider==="shopify")await shopifyGraphql(`query Connection{shop{id name}}`);else if(provider==="kiotviet")await getKiotVietAccessToken(true);else return Response.json({success:false},{status:404});await query("UPDATE integrations SET status='connected',last_health_check_at=now(),updated_at=now() WHERE provider=$1",[provider]);return Response.json({success:true});}catch(error){return Response.json({success:false,error:error instanceof Error?error.message:"Connection failed"},{status:502});}}
