import { z } from "zod";import { requireAdmin } from "@/lib/auth/middleware";import { assertTrustedOrigin } from "@/lib/security/csrf";import { query } from "@/lib/db/client";
const schema=z.object({resolution:z.record(z.string(),z.unknown()).default({}),status:z.enum(["resolved","ignored"])});
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){try{await requireAdmin();assertTrustedOrigin(request);const{id}=await params,input=schema.parse(await request.json());await query("UPDATE sync_conflicts SET resolution_status=$2,resolution=$3,resolved_at=now() WHERE id=$1",[id,input.status,JSON.stringify(input.resolution)]);return Response.json({success:true});}catch(error){return Response.json({success:false,error:error instanceof Error?error.message:"Action failed"},{status:400});}}
