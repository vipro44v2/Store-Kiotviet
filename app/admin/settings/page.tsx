import { connection } from "next/server";import { query } from "@/lib/db/client";import { SettingsEditor } from "@/components/admin/settings-editor";
export default async function SettingsPage(){await connection();let settings:Array<{key:string;value:Record<string,unknown>}>=[],error="";try{settings=await query("SELECT key,value FROM system_settings ORDER BY key");}catch(e){error=e instanceof Error?e.message:"Database unavailable";}return <><header className="admin-header"><div><p className="eyebrow">Configuration</p><h1>System settings</h1></div></header>{error&&<div className="error-banner">{error}</div>}<SettingsEditor settings={settings}/></>}
